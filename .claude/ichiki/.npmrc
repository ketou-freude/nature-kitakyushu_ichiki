# puppeteer（pa11y の依存）は postinstall で Chrome を丸ごと落とす。
# 実測: 500MB 超のダウンロードが npm install の最中に走り、
# ネットワークやプロキシで落ちると **毎回** setup が失敗する。
#
# Ichiki は playwright の chromium を別途入れており、pa11y はそれで動く
# （src/a11y/check.js が executablePath を渡す）。二重に持つ理由が無い。
puppeteer_skip_download=true
