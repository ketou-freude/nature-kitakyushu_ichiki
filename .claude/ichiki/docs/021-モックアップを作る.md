# モックアップを作る・直す

> [案件のトップに戻る](../../../README.md)（やりたいことから探せます）

お客様と合意するまで、モックは何度も触ります。**見た目が変わるのは正しいこと**です。  
守るのは1つだけ、**`lint` が通っていること**です。

AI に書かせて lint を通す回し方は  
[023-AIに書かせてlintを通す.md](023-AIに書かせてlintを通す.md) にまとめてあります。  
**この文書には、新規に書くときだけ違うことを書きます。**

---

## 見た目だけ合意済みのモックが降りてきたとき

宣言（`data-*`）が付いていないモックを渡されることがあります。  
その場合は**構造化**という別の作業になります。見た目が変わってはいけないので、  
手順が普段と違います。

→ [022-既存htmlからモックアップを作る.md](022-既存htmlからモックアップを作る.md)

---

## 見た目を変えたら怒られる？

怒られません。合意までは見た目が変わるのが正しいので、  
ピクセル比較は `gate` に入れていません。

見た目を固定するのは、**既存モックを制約語彙に直す（構造化）ときだけ**です。

---

## 完成の目安

- [ ] `lint` の error が 0
- [ ] `L20` の一覧に目を通した（更新できない文言の合意）
- [ ] お客様が合意した

ここまで来たら [03-変換して検査する.md](03-変換して検査する.md) へ進みます。
